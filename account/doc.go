// Business layer
// Account package would manage all the business logics behind the API
// This module would be called by the higher level (api package), and it satisfies the api package requirements
// The package declares and only relies on its requirements (interfaces) that lower-level modules should implement
// TODO enhance documentation
package account
